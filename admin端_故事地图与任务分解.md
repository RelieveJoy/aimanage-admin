# 爱管理 · 管理员端（Admin）故事地图与任务分解

> 版本：v1.0　|　日期：2026-09-13　|　归属：Epic AD（独立于 Epic A–D 的第四端）
> 配套文档：《22_项目规划方案.docx》第五章（角色权限）、第四章（MoSCoW）、第六章（用户故事地图）、第七章（Sprint 切片）

---

## 0. 一句话定位

**管理员是购买了这套软件的那家公司的企业管理员**——管人（PM 与成员）、管组织、管证据链；对业务数据**只看不写**。

依据（第一次课课堂原话）：

> "这个平台本身是你会支持**某一个企业的很多个项目**，所以你要考虑它的……就是**多项目的指导**。"
> —— 第一次课.txt:171

因此本端为**单企业、多项目**形态，**不做多租户设计**（无 `tenant_id`，无企业切换，无企业管理页）。

---

## 1. 范围边界

### ✅ In Scope

| 域 | 内容 |
|---|---|
| 认证 | 独立登录页，复用后端认证接口 |
| 用户 | 创建 PM / Member、列表检索、改角色、改部门、启停、重置密码 |
| 组织 | 部门树（两层：公司 → 部门）、部门 CRUD、成员归属 |
| 项目 | 全公司项目列表、新建项目、指定/更换 PM、归档 |
| 成员 | 项目成员增删、成员变更历史 |
| 审计 | 全局审计账本多条件检索、前后值对比、导出 |
| 观测 | 看板 / 甘特**只读**视图 |
| 审批 | PM 加人申请审批、面向 Admin 的通知中心 |

### ❌ Out of Scope

| 不做 | 依据 |
|---|---|
| 权限配置页（增删角色、勾选权限点） | 方案 Table 5 明确列为 **Won't**——"角色体系生命周期内稳定，配置界面属过度设计（YAGNI）" |
| 任务的增删改、排期调整、拖拽看板 | 归 PM 端；Admin 写业务数据会破坏三角色模型，且污染责任判定叙事 |
| 国密算法（SM2/SM3/SM4） | 本期先做普通账号密码登录，后期与团队同步 |
| AI 相关工作台 / Agent 名册 | 对齐方案 Epic D：本次只留数据结构与接口，功能不做 |
| 多租户 / 多企业管理 | 单企业形态（见第 0 节） |
| 全量通知订阅（所有人的通知都收） | 只收**面向 Admin** 的通知 |
| 开放注册页 | 账号只能由管理员创建（方案第五章"写严格"） |

---

## 2. 故事地图（Epic AD）

### AD1 管理员登录与入口隔离

| 核心故事 | 细分任务 | 设计思考 |
|---|---|---|
| **AD1 管理员登录与入口隔离**<br>独立登录页，非管理员被挡在门外 | AD1.1 独立登录页 ｜ AD1.2 JWT 存储 + 请求拦截器 ｜ AD1.3 前端路由守卫（`role != ADMIN` 拒绝） ｜ AD1.4 后端 `@RequireRole(ADMIN)` 拦 `/api/admin/**` ｜ AD1.5 401/403 统一处理 | 只做 **UI 独立**，认证接口完全复用 `POST /api/auth/login`；AD1.2 与 AD1.4 分别复用业务端 API client 与 A1.2 的 AOP 中间件——**这两项是"同仓库"的直接收益，不算额外工作量**。AD1.3 只是体验层，**安全边界在 AD1.4** |
| **AD1.T 验收动作** | 用 Member 的 token 直接请求 `/api/admin/users` | **必须返回 403**。方案 DoD 第 2 条明文要求"Member 越权写操作被正确拒绝" |

**优先级：Must**（Sprint 1）

---

### AD2 用户账号管理

| 核心故事 | 细分任务 | 设计思考 |
|---|---|---|
| **AD2 用户账号管理**<br>创建并管理这家公司的所有 PM 与成员 | AD2.1 用户列表（搜索/角色/部门/状态筛选 + 分页） ｜ AD2.2 **新建 PM 账号** ｜ AD2.3 **新建 Member 账号** ｜ AD2.4 编辑：改角色、改部门 ｜ AD2.5 启用/停用 ｜ AD2.6 重置密码 | 角色是**三值枚举而非权限配置**——AD2.2/AD2.3 是同一个表单的两个入口，不是两套逻辑。创建时角色下拉**只给 PM / MEMBER**，Admin 不可通过界面授予。停用用户保留全部审计记录，只失去登录能力 |

**优先级：Must**（Sprint 1）

---

### AD3 组织架构

| 核心故事 | 细分任务 | 设计思考 |
|---|---|---|
| **AD3 组织架构**<br>看清这家公司的部门与人员归属 | AD3.1 部门树（**两层：公司 → 部门**） ｜ AD3.2 部门 CRUD ｜ AD3.3 成员归属调整 ｜ AD3.4 树 + 成员混排视图 | 单企业形态下，**树根就是本公司**，不设"企业"层。方案 A3.1 原写"递归树组件"，此处**有意简化为两层**：课设场景下三层以上无真实收益，且是资源负载热力图（B5）的数据基础，需先落地。AD3.4 左树右人，一屏看完 "谁在哪个部门" |

**优先级：Must**（Sprint 1）

---

### AD4 项目管理

| 核心故事 | 细分任务 | 设计思考 |
|---|---|---|
| **AD4 项目管理**<br>新建项目并指定负责人 | AD4.1 全公司项目列表（**直接显示 PM 姓名**、成员数、状态） ｜ AD4.2 新建项目（名称 + 指定 PM） ｜ AD4.3 更换 PM ｜ AD4.4 归档项目 | 单企业，**列表无需企业筛选，一屏到底**。AD4.2 的 PM 必须从 `role=PM` 的用户中选（约束见 FR-045）。AD4.3 是高危操作（换帅），必须写审计。**归档 ≠ 删除**，数据与审计链全留 |

**优先级：Must**（Sprint 2）

---

### AD5 项目成员与成员变化

| 核心故事 | 细分任务 | 设计思考 |
|---|---|---|
| **AD5 项目成员与成员变化**<br>明确每个项目的 PM 和成员是谁，并追踪成员进出 | AD5.1 项目详情：**PM 是谁 + 成员列表 + 各自角色** ｜ AD5.2 直接加人 / 移人 ｜ AD5.3 **成员变更历史**（谁、何时、进/出） ｜ AD5.4 按人反查：此人参与了哪些项目 | **成员挂在项目下**（`project_member`），不挂在 PM 名下——已与需求方确认。AD5.3 是**白捡的**：`project_member` 是一张有写入的表，A2 的全链路审计 AOP 会自动记录增删，本端只需做展示，**无需单独设计埋点**。AD5.4 是责任判定时最常用的反向视角 |

**优先级：Must**（Sprint 2）

---

### AD6 全局审计账本检索 ★ 核心价值

| 核心故事 | 细分任务 | 设计思考 |
|---|---|---|
| **AD6 全局审计账本检索**<br>三秒出示证据，把"查无对证"变成"当场举证" | AD6.1 多条件检索（项目/操作人/对象类型/字段/时间区间/关键字） ｜ AD6.2 列表 + 详情抽屉，**前后值左右对比高亮** ｜ AD6.3 按项目 / 按人两个快捷入口 ｜ AD6.4 导出 CSV | 本端**唯一的差异化展示面**，直接对应方案特色一。AD6 是 `audit_log` 的**只读侧**；写入侧由 A2 的 AOP 承担（**跨端依赖，见第 4 节**）。AD6.2 的前后值对比必须做显眼——方案 Table 4 点名竞品"事件流但无前后值对比"，这是代差所在。检索结果**不可修改、不可删除**，Admin 自己也无权 |

**优先级：Must**（Sprint 3）　**AD6.4 导出为 Should，时间紧可砍**

---

### AD7 只读观测（看板 + 甘特）

| 核心故事 | 细分任务 | 设计思考 |
|---|---|---|
| **AD7 只读观测**<br>盯进度但不插手 | AD7.1 按项目进入的看板只读视图 ｜ AD7.2 甘特只读视图 ｜ AD7.3 "观察模式"标识 | **复用业务端组件，传 `readonly=true`，不重写**——这是本端最大的省工点，省掉约一个 Sprint 的前端量。只读必须是真的只读：**不渲染拖拽手柄、不发写请求**；前端藏不够，**后端对 Admin 的写请求也要拒绝**（否则越权）。AD7.3 让管理员清楚自己在看别人的项目 |

**优先级：Must**（Sprint 2）

---

### AD8 加人申请审批 + 通知中心

| 核心故事 | 细分任务 | 设计思考 |
|---|---|---|
| **AD8 加人申请审批 + 通知中心**<br>PM 提申请，管理员拍板 | AD8.1 待审批列表（PM 申请给项目加人） ｜ AD8.2 批准 / 拒绝 + 填写意见 ｜ AD8.3 通知中心（**只收面向 Admin 的通知**） ｜ AD8.4 未读计数 + 已读标记 | 本期**只做这一个事件源**，一张 `request` 表即可，不做多事件源矩阵。AD8 顺手解决了权限边界问题：**PM 不能自行加人，须经管理员审批**——这使"组织架构 + 项目成员"完全归属本端，与"明确每个项目的 PM 和成员是谁"的诉求严丝合缝。AD8.2 批准后自动写 `project_member`，且**批准动作本身也写入审计**。AD8.3 有意只收 Admin 通知，**不退化为全量通知订阅** |

**优先级：Must（AD8.1–AD8.2） / Should（AD8.3–AD8.4）**（Sprint 3）

---

### AD9 概览面板

| 核心故事 | 细分任务 | 设计思考 |
|---|---|---|
| **AD9 概览**<br>进入管理端第一眼看到全局 | AD9.1 用户数 / 项目数 / 今日变更数 / 待审批数 | 纯聚合查询，无新表。**Can 级：时间紧直接砍，不影响任何验收项** |

**优先级：Could**（Sprint 3，可砍）

---

## 3. Sprint 切片

| Sprint | 交付故事 | 排期理由 |
|---|---|---|
| **Sprint 1**<br>数字地基（第 1–2 周） | AD1 登录与入口隔离<br>AD2 用户账号管理<br>AD3 组织架构 | 三项均为方案 Epic A 的正式内容，与后端 A1（三角色权限）、A3（组织架构树）同批交付，共用同一套数据模型，返工风险最低 |
| **Sprint 2**<br>实时协作空间（第 3–4 周） | AD4 项目管理<br>AD5 项目成员与成员变化<br>AD7 只读观测 | 依赖业务端已有真实数据可看（Sprint 2 才产出任务与需求），此时接入观测才有内容；AD5 的成员变更历史也开始积累 |
| **Sprint 3**<br>数据洞察与商业级交付（第 5–6 周） | AD6 全局审计账本检索<br>AD8 申请审批 + 通知<br>（AD9 概览，可砍） | **审计从 Sprint 1 起就在后台持续写入，攒到第 5 周才有检索价值**——这与方案 C1（ProjectGit）的排期逻辑完全一致："最后做不是最不重要，而是唯有此时做才有意义" |

---

## 4. ⚠️ 跨端依赖（需与其他成员同步）

本端有 3 个依赖不在自己手里，**不提前对齐会返工**：

| # | 依赖 | 需要对方提供 | 影响 |
|---|---|---|---|
| 1 | **只读观测** | 业务端看板/甘特组件支持 `readonly` 模式；后端放行 ADMIN 的 GET、拒绝其写请求 | AD7 全部 |
| 2 | **加人申请** | PM 端提供"申请给项目加人"入口，调用 `POST /api/requests` | AD8.1 的数据来源 |
| 3 | **审计写入（A2 的 AOP 切面）** | 属**共享后端**，不归属任何端——**必须确认由谁实现** | AD6 全部（它不写，本端第 5 周检索为空） |

> **第 3 条最要紧**：方案表 8 将 A2 列为 Epic A 内容，但未指定归属。建议尽快向团队确认"A2 审计切面谁写"。

---

## 5. 验收标准（对齐方案 10.2 DoD）

| # | 标准 | 验证方式 |
|---|---|---|
| 1 | 全部 Must 故事可演示 | Sprint 3 现场走查 |
| 2 | **非 ADMIN 角色调用 `/api/admin/**` 被 403 拒绝** | 取 Member token 直连接口（对应方案 DoD 第 2 条） |
| 3 | **Admin 对业务的写请求被拒绝** | 构造改任务的请求，须被拒 |
| 4 | 审计账本覆盖全部写操作 | 造一次状态变更，检索能查到，且前后值完整（对应方案 DoD 第 4 条） |
| 5 | 关键路径有测试用例 | 需在 TASKS/测试计划中补齐 |
| 6 | 文档齐全 | 本文档 + 接口契约 + 数据库表结构 |

---

## 6. 演示脚本（Sprint 3 验收）

**总时长约 3 分钟，一条主线走完本端全部价值。**

1. **登录** → 用管理员账号进管理端（`/login` 独立入口）
2. **建人** → AD2 新建一个 PM、一个 Member
3. **建组织** → AD3 建部门，把两人放进去
4. **建项目** → AD4 新建项目、指定刚才那位 PM
5. **配成员** → AD5 把 Member 加进项目，展示 PM 与成员一眼可见
6. **观测** → AD7 进入该项目看板（只读），说一句"我能看，但改不了"
7. **审批** → AD8 收到 PM 的加人申请，批准
8. **举证** → AD6 检索该项目，定格在那条记录上：

> **8月12日 14:32 · 张三(PM) · 爱管理项目 · 需求#12 · status · "已确认" → "待评审" · 备注"客户方王总电话确认"**

> 台词："客户问需求什么时候被改的——不用翻聊天记录，三秒出示证据。"

这段直接对应方案特色一的使用体验原文。

---

## 7. 待办与待确认

| # | 事项 | 状态 |
|---|---|---|
| 1 | 成员挂项目还是挂 PM | ✅ 已确认：**挂在项目下**（`project_member`） |
| 2 | 国密算法 | ⏸ 本期不做，后期与团队同步 |
| 3 | AI 相关工作台 | ⏸ 本期不做，对齐 Epic D 只留接口 |
| 4 | 通知事件源范围 | ✅ 已确认：**方案 B**（仅"PM 申请加人"单一事件源） |
| 5 | **A2 审计切面由谁实现** | ⬜ 待向团队确认（跨端依赖第 3 条） |
| 6 | 技术选型与目录结构 | ⬜ 待定（建议下一份文档：ARCHITECTURE.md） |
| 7 | 接口契约与表结构 | ⬜ 待定（建议下一份文档：API_SPEC.md / schema.sql） |
